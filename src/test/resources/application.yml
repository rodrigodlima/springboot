spring:
  application:
    name: be-charge
  data:
    mongodb:
      uri: mongodb://localhost:27017/be-charge

security:
  basic:
    enabled: false

app:
  rabbitmq:
    ttlInMilliseconds: ${rabbitmq.ttlInMilliseconds:3600000} # 1 hour
    delayInMilliseconds: ${rabbitmq.delayInMilliseconds:900000} # 15 minutes
    maximumNumberOfRetries: ${rabbitmq.maximumNumberOfRetries:3}

info:
  build:
    groupId: br.com.getnet.credenciamentodigital
    artifactId: sample-ms
    version: 1.0.0
    date: "2018-12-12 12:12:12"